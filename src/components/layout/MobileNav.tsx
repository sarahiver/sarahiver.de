/**
 * DEPRECATED: MobileNav wird nicht mehr genutzt seit Landing v2.
 * Die neue page.tsx nutzt <SiteHeader> aus layout/SiteHeader.tsx.
 * Diese Datei darf gelöscht werden — bleibt nur als no-op damit
 * Next.js Type-Check nicht crasht.
 */
export default function MobileNav() {
  return null;
}
